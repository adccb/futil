{
  "name": "futil",
  "version": "1.0.0",
  "description": "a tiny functional utility library",
  "main": "dist/main.js",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.0",
    "babel-preset-flow": "^6.23.0",
    "flow-bin": "^0.63.1",
    "jasmine": "^2.8.0"
  },
  "scripts": {
    "build": "babel src/ -d dist/",
    "flow": "flow",
    "prepublish": "npm run build",
    "test": "npm run build && npm run flow && jasmine"
  },
  "keywords": [],
  "author": "autumn crossan",
  "license": "ISC"
}
